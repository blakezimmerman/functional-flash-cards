{
  "name": "functional-flash-cards",
  "version": "1.0.0",
  "description": "flash card web app written in elm",
  "scripts": {
    "start": "elm-live src/Main.elm --output=main.js --open"
  },
  "dependencies": {
    "elm": "^0.18.0"
  },
  "devDependencies": {
    "elm-live": "^2.7.4"
  }
}
